<p id="notice"><%= notice %></p>

<h1>Breweries</h1>
<p> Number of breweries: <%= @breweries.count %> </p>
<h2>Active</h2>
<p>Number of active breweries: <%= @active_breweries.count %> </p>
<!--<table class="table table-hover">
  <thead>
    <tr>
      <th>Name</th>
      <th>Year</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @active_breweries.each do |brewery| %>
      <tr>
        <td><%= link_to brewery.name, brewery %></td>
        <td><%= brewery.year %></td>
        <td><%= link_to 'Edit', edit_brewery_path(brewery) if current_user %></td>
        <td><%= link_to 'Destroy', brewery, method: :delete, data: { confirm: 'Are you sure?' } if current_user %></td>
      </tr>
    <% end %>
  </tbody>
</table>-->
<% if @active_breweries.count > 0 %>
  <%= render 'list', breweries: @active_breweries %>
<% end %>
<h2>Retired</h2>
<p>Number of retired breweries: <%= @retired_breweries.count %> </p>

<% if @retired_breweries.count > 0 %>
  <%= render 'list', breweries: @retired_breweries %>
<% end %>

<br>
<% if current_user %>
  <%= link_to 'New Brewery', new_brewery_path, class:"btn btn-info"%>
<% end %>
<%= link_to 'List of beers', beers_path, class:"btn btn-info" %>
