<p id="notice"><%= notice %></p>

<p>
  <h2><%= @brewery.name %></h2>
</p>

<p>
  <i>Established in <%= @brewery.year %></i>
</p>

<p>
  Number of beers <%= @brewery.beers.count %>
</p>

<!-- Tämä printtaa kaikki tiedot tämän panimen oluista <p>
  <%= @brewery.beers.to_a %>
</p> -->

<p>
  <% all_ratings = 0 %>
  <% @brewery.beers.each do |beer| %>
    <%= link_to beer.name, beer %><!--, has <%= beer.ratings.count %> ratings: -->
    <% all_ratings += beer.ratings.count %>
    <% if beer.ratings.empty? %>
      
    <% else %>
      <% beer.ratings.each do |rat| %>
<!--        <%= rat.score %>, -->
      <% end %>
    <% end %>
    <br>
  <% end %>
</p>

<p>
  Has <%= pluralize(all_ratings,'rating') %>.
  <% if all_ratings == 0%>

  <% else %>
    Average <%= @brewery.average_rating.round(2) %>
  <% end %>
</p>

<%= link_to 'Edit', edit_brewery_path(@brewery) if current_user %> <!--|
<%= link_to 'Back', breweries_path %> -->